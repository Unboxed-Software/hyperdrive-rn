import React from 'react';
import RNPickerSelect from 'react-native-picker-select';

const Select = (props: React.ComponentProps<typeof RNPickerSelect>) => <RNPickerSelect {...props} />;

export default Select;
